name: chatting_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  chat_bubbles: ^1.5.0

  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  gap:
  get:
  get_storage:
  image_picker: ^0.8.5+3

dev_dependencies:
  flutter_lints: ^2.0.0

  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/lang/
    - assets/images/
